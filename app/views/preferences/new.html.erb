<div class="mx-auto" style="width: 80%; color:white">
  <h1 class="fs-2 my-2">My match preference</h1>

  <%= simple_form_for([@user, @preference]) do |f| %>
    <%= f.error_notification %>

    <div class="form-inputs">
      <%= f.input :max_age, label: "Max Age", as: :intege, required: true%>
      <%= f.input :min_age, label: "Min Age", as: :integer, required: true%>
      <%= f.input :max_distance, label: "Distance (km)", as: :integer, required: true%>
      <%= f.input :gender, collection: ["M","F","Other"], as: :radio_buttons, required: true%>
    </div>

    <div class="form-actions">
      <%= f.button :submit, "Save", class:"btn border-success my-3" %>
    </div>
  <% end %>
</div>
